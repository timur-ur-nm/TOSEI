import React from 'react'

export default function useFilterCar(sortedCars, filter) {
    
    // if (filter.avaliable) return sortedCars.filter((car)=>)


    // const filteredAndSortedCars = sortedCars.filter((a,b)=> {})

    // return { filteredAndSortedCars }
}
